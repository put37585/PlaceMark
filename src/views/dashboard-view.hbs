{{> menu active="dashboard"}}

<section class="section">
    <div class="tile is-ancestor">
        <div class="tile is-vertical is-8 mr-3">
            {{> list-pois}}
        </div>
        <div class="tile is-4 ml-3">
            {{> add-poi}}
        </div>
    </div>
</section>
